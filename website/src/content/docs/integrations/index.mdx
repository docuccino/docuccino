---
title: Supported integrations
description: Every built-in integration, what each one documents, what it needs, and how it activates.
sidebar:
  label: Overview
  order: 1
---

import { Aside, LinkCard, CardGrid } from '@astrojs/starlight/components';

Integrations are how Docuccino understands the packages and conventions you already use. Each one
reads a specific part of your app — Form Requests, API Resources, Eloquent models, query builders,
authentication — and contributes the matching pieces of your documentation.

You never switch them on. Each integration activates the moment it's relevant: the Spatie Data
support the moment `spatie/laravel-data` is installed, the Passport support the moment Passport is,
and so on. Anything that builds on Laravel itself — validation, Form Requests, resources, models,
enums, error responses, rate limiting — is always available.

<Aside title="Built with the tools you have">
Every integration on this page is built using the same public extension API that's available to
you — you have access to everything Docuccino used to build itself. Nothing here is privileged: if
an integration can read a package, so can one you write. See
[Writing an integration](/guides/extension-authoring/).
</Aside>

## The integration matrix

Each row links to its detailed page. "Requires" tells you what has to be installed for the
integration to activate; "Config" links the knobs that tune it.

### Request &amp; validation

| Integration | What it documents | Requires | Config |
| --- | --- | --- | --- |
| [Validation rules](/integrations/requests/) | Laravel's validation DSL turned into schema constraints — types, formats, sizes, presence, and enumerations | Built in — always on | — |
| [Form Requests &amp; inline `validate()`](/integrations/requests/) | Request bodies and query parameters from a Form Request's `rules()`, or an inline `$request->validate([...])` / `Validator::make(...)` | Built in — always on | — |
| [Spatie Data](/integrations/requests/) | Data classes as reusable schemas, and request bodies from Data-typed action parameters | [`spatie/laravel-data`](https://spatie.be/docs/laravel-data) | — |
| [Laravel Actions](/integrations/laravel-actions/) | Action classes as controllers — the resolved `handle()`/`asController()` signature, a request body from `rules()`, and a `403` from `authorize()` | [`lorisleiva/laravel-actions`](https://laravelactions.com) | — |

### Responses, models &amp; schemas

| Integration | What it documents | Requires | Config |
| --- | --- | --- | --- |
| [API Resources](/integrations/schemas/) | Response schemas from `JsonResource` shapes — plus Laravel's first-party JSON:API resources | `JsonResource` built in; JSON:API auto-activates on Laravel 13 | [`integrations.api_resources.wrap`](/reference/configuration/#integrations) |
| [JSON:API resources (timacdonald)](/integrations/timacdonald-json-api/) | Pre-13 JSON:API documents from `timacdonald/json-api` resources — identical output to the first-party support, plus `include` / `fields[TYPE]` | [`timacdonald/json-api`](https://github.com/timacdonald/json-api) | — |
| [Eloquent models](/integrations/schemas/) | Model schemas from columns, refined by `$visible` / `$hidden` / `$appends` / casts — including polymorphic `MorphTo` as a discriminated `oneOf` | Built in — always on | — |
| [Enums](/integrations/schemas/) | Backed enums by their backing values, `#[CaseDescription]` prose as `x-enumDescriptions`, and optional codegen name hints | Built in — always on | [`representation.enums.naming`](/reference/configuration/#representation) |

### Error responses

| Integration | What it documents | Requires | Config |
| --- | --- | --- | --- |
| [Inferred exception handlers](/integrations/error-responses/) | 4xx/5xx responses from your *real* error handling — render callbacks, exception `render()`, and `Responsable` exceptions | Built in — always on | [`error_responses`](/reference/configuration/#error_responses) |
| [Problem Details preset](/integrations/error-responses/) | RFC 9457 `application/problem+json` error responses | Built in — opt-in | [`error_responses: 'problem-details'`](/reference/configuration/#error_responses) |
| [Framework error defaults](/integrations/error-responses/) | Laravel's stock JSON error shapes — `{message, errors}` for 422, `{message}` for 401/403/404 | Built in — always on | [`error_responses`](/reference/configuration/#error_responses) |

### Authentication &amp; operations

| Integration | What it documents | Requires | Config |
| --- | --- | --- | --- |
| [Sanctum](/integrations/security/) | Bearer-token and stateful-cookie security schemes — both modes when a single route carries both | [`laravel/sanctum`](https://laravel.com/docs/sanctum) | [`integrations.sanctum`](/reference/configuration/#integrations) |
| [Passport](/integrations/security/) | An OAuth2 security scheme with per-operation scopes read from `scope:` / `scopes:` middleware | [`laravel/passport`](https://laravel.com/docs/passport) | [`integrations.passport.url`](/reference/configuration/#integrations) |
| [Permissions](/integrations/security/) | `role:` / `permission:` / `role_or_permission:` middleware as an `x-permissions` member and a description line | [`spatie/laravel-permission`](https://spatie.be/docs/laravel-permission) | — |
| [Rate limiting](/integrations/rate-limiting/) | A `429` response with `Retry-After` and `X-RateLimit-*` headers, read from `throttle` middleware | Built in — always on | — |
| [Query Builder](/integrations/query-builder/) | Filters, sorts, includes, sparse field sets, and pagination — traced through the builder at any call depth | [`spatie/laravel-query-builder`](https://spatie.be/docs/laravel-query-builder) | [`integrations.query_builder`](/reference/configuration/#integrations) |
| [JSON API Paginate](/integrations/json-api-paginate/) | JSON:API `page[number]` / `page[size]` (or `page[cursor]`) pagination parameters from `jsonPaginate()` | [`spatie/laravel-json-api-paginate`](https://github.com/spatie/laravel-json-api-paginate) | — |

### Quality

| Integration | What it documents | Requires | Config |
| --- | --- | --- | --- |
| [Data-leakage lint](/reference/configuration/#lint) | Warns when a schema property name looks sensitive (`password`, `token`, `secret`, `api_key`, …) before it ships — a diagnostic, never a mutation | Built in — always on | [`lint.leakage`](/reference/configuration/#lint) |

## How contributions combine

Several sources can describe the same operation — an inferred type, an integration, a docblock, an
attribute. Docuccino merges them with a clear order of precedence, and higher layers win field by
field (an attribute can override one field of a response without discarding the rest):

```
inference  <  integration  <  docblock  <  attribute  <  overlay  <  config
```

So an integration's best guess is always something you can refine with a docblock or an
[attribute](/reference/attributes/) — you're never fighting the tool.

Every contribution is recorded with its source in the document's provenance (visible in the raw
[UIR](/uir/)), labeled by the integration that made it — for example `integration:query-builder` or
`integration:sanctum`. That's what lets tooling explain *why* a given detail is documented the way
it is.

<CardGrid>
	<LinkCard title="Write your own integration" href="/guides/extension-authoring/" description="Every integration above is built with tools you have. Build one for your own package end to end." />
	<LinkCard title="Configuration reference" href="/reference/configuration/" description="Every config key, its default, and an example." />
</CardGrid>
